# Sleep after 1 hour (1*60*60*1000ms)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

# Increase Bluetooth TX power for better range
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Display off (no e-ink on Crosses35)
CONFIG_ZMK_DISPLAY=n
